package org.application.core.elements;

public class HtmlBody extends Element {
    protected HtmlBody(Document document, Object pointer) {
        super(document, pointer, ElementType.HTML_BODY);
    }
}
